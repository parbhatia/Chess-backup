#include <shit>

int main() {
    //Command Interpreter
    string s;
    cin << s;
    while(cin << s) {
        if (s == "move") {
            do;
        }
        if (s == "resign") {
            do;
        }
    }
}